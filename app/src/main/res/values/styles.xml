<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:icon">@drawable/ic_more_vert_black_24dp</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="CollapsedAppBar" parent="@android:style/TextAppearance.Medium">
        <item name="android:textSize">24sp</item>
        <item name="android:textColor">#ffffff</item>
        <item name="android:textStyle">normal</item>
        <item name="android:fontFamily">@font/alojalight</item>
    </style>

    <style name="ExpendedAppBar" parent="@android:style/TextAppearance.Material.Title">
        <item name="android:textColor">#000000</item>
        <item name="android:textStyle">normal</item>
        <item name="android:fontFamily">@font/alojalight</item>

    </style>

    <style name="ToolBarTextAppearance">
        <item name="android:fontFamily">@font/alojalight</item>
        <item name="android:icon">@drawable/ic_more_vert_black_24dp</item>
    </style>

    <style name="SearchBarTextAppearance">
        <item name="android:fontFamily">@font/briberra</item>
        <item name="android:textSize">20sp</item>
    </style>

    <style name="NavigationViewItem">
        <item name="android:fontFamily">@font/briberra</item>
        <item name="android:textSize">25sp</item>
    </style>

</resources>
